# Review Correction

Use a temporary fixture containing a controlled implementation defect.

Assert that Reviewer receives the exact baseline and accepted decisions, reports a confirmed evidence-backed finding, the main agent resumes the same Coder with bounded acceptance, invalidated checks rerun, and the same Reviewer performs re-review.
