# Claim-Only Rejection

Create a case where a worker claims that tests passed without persisted command evidence.

Assert that the main agent treats the statement as an unsupported claim, does not advance the acceptance stage, and does not report verified success.
