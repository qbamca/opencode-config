# Lifecycle Reconciliation

Create a case with a required child left running, cancelled, or otherwise unreconciled.

Assert that the main agent reports `partial`, `blocked`, or `cancelled`, identifies the unresolved ledger entry, and never reports success.
