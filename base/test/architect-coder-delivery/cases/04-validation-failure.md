# Validation Failure

Use a bounded validation command that intentionally exits nonzero.

Assert that transport completion is not semantic success, the exact failure evidence reaches the main agent, required diagnosis returns to the same Coder when appropriate, and success is impossible until affected validation passes.
