# Plan Contradiction

Use a temporary fixture whose observable interface conflicts with one accepted plan decision.

Assert that Coder returns `contradiction` with exact evidence, the main agent resumes the same Architect task, accepts a new plan version, resumes the same Coder task with the complete amendment, and no silent architecture change occurs.
